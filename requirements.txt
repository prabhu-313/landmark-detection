# LandMark — Real-Time Facial Landmark Detection
# Dependencies

# CRITICAL: numpy<2 required — Dlib 19.x bindings are incompatible with NumPy 2.x

# Core vision and detection
opencv-python>=4.8.0
dlib>=19.24.0
imutils>=0.5.4

# For EAR drowsiness extension (Section 7)
scipy>=1.11.0

# For GPU benchmark section (Section 8) — optional, comment out if not using GPU
# torch>=2.0.0
# torchvision>=0.15.0
